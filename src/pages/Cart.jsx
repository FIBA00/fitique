/* Lavender Lookbook cart: a calm, editable fitting-room bag with all decision-making details visible at a glance. */
import { ArrowRight, Heart, Minus, Plus, ShoppingBag, Trash2 } from "lucide-react";
import { Link, useNavigate } from "react-router";
import { useState } from "react";
import { toast } from "sonner";
import EmptyState from "../components/feedback/EmptyState";
import CartSupportPanel from "../components/commerce/CartSupportPanel";
import { useCartStore } from "../features/products/hooks/useCart";
import { formatCurrency } from "../lib/utils";
import { usePageMeta } from "../hooks/usePageMeta";

export default function Cart() {
  usePageMeta("Your bag", "Review and edit the Fitique pieces you have selected.");
  const { items, updateQuantity, removeItem, clearCart, promotionCode, deliveryPreference } = useCartStore(); const [confirming, setConfirming] = useState(false); const navigate = useNavigate();
  const subtotal = items.reduce((total, item) => total + item.price * item.quantity, 0); const delivery = subtotal ? 12 : 0; const total = subtotal + delivery;
  if (!items.length) return <div className="content-shell py-10 lg:py-16"><EmptyState icon={ShoppingBag} title="Your bag is waiting." description="A considered piece is a good place to begin. Browse the current edit, then come back when something feels right." /></div>;
  return <div className="content-shell py-8 lg:py-12"><div className="grid gap-5 border-b border-fitique-line pb-6 sm:grid-cols-[1fr_auto] sm:items-end"><div><p className="eyebrow text-fitique-brown">Your selections</p><h1 className="serif mt-2 text-5xl tracking-[-.04em] text-fitique-plum">Your bag</h1><p className="mt-3 max-w-lg border-l-2 border-fitique-plum pl-3 text-sm leading-6 text-fitique-ink/60">An outfit in progress. Keep the pieces that feel like a natural next chapter.</p></div><button onClick={() => setConfirming(true)} className="focus-ring hidden items-center gap-2 text-xs font-bold text-fitique-brown hover:text-fitique-plum sm:flex"><Trash2 size={14} /> Clear bag</button></div>{confirming && <div role="alertdialog" aria-modal="true" aria-labelledby="clear-title" className="fixed inset-0 z-50 grid place-items-center bg-fitique-ink/40 p-4"><div className="w-full max-w-sm border border-fitique-line bg-fitique-ivory p-6 soft-shadow"><p className="eyebrow text-fitique-brown">A small pause</p><h2 id="clear-title" className="serif mt-2 text-3xl text-fitique-plum">Clear your bag?</h2><p className="mt-3 text-sm leading-6 text-fitique-ink/65">Your selected pieces will be removed from this device.</p><div className="mt-6 flex gap-3"><button onClick={() => setConfirming(false)} className="ghost-button focus-ring flex-1">Keep pieces</button><button onClick={() => { clearCart(); setConfirming(false); toast("Your bag is clear"); }} className="plum-button focus-ring flex-1">Clear bag</button></div></div></div>}<div className="mt-8 grid gap-10 lg:grid-cols-[1fr_22rem]"><section><div className="grid gap-5">{items.map((item) => <article key={item.lineId} className="grid grid-cols-[7rem_1fr] gap-4 border-b border-fitique-line pb-5 sm:grid-cols-[8.5rem_1fr]"><Link to={`/products/${item.id}`} className="focus-ring aspect-[.78] overflow-hidden bg-fitique-paper"><img src={item.image} alt={item.name} className="h-full w-full object-cover saturate-[.74] sepia-[.045]" /></Link><div className="flex min-w-0 flex-col"><div className="flex justify-between gap-4"><div><p className="eyebrow text-fitique-brown">{item.category}</p><Link to={`/products/${item.id}`} className="focus-ring mt-1 block text-sm font-extrabold hover:text-fitique-plum">{item.name}</Link><p className="mt-1 text-xs text-fitique-ink/55">{item.color} · {item.size}</p></div><p className="shrink-0 text-sm font-extrabold text-fitique-plum">{formatCurrency(item.price * item.quantity)}</p></div><div className="mt-auto flex items-center justify-between pt-4"><div className="inline-flex border border-fitique-line bg-white"><button onClick={() => updateQuantity(item.lineId, item.quantity - 1)} aria-label={`Decrease ${item.name} quantity`} className="focus-ring grid h-8 w-8 place-items-center text-fitique-plum"><Minus size={14} /></button><span className="grid w-8 place-items-center border-x border-fitique-line text-xs font-bold">{item.quantity}</span><button onClick={() => updateQuantity(item.lineId, item.quantity + 1)} aria-label={`Increase ${item.name} quantity`} className="focus-ring grid h-8 w-8 place-items-center text-fitique-plum"><Plus size={14} /></button></div><button onClick={() => { removeItem(item.lineId); toast("Removed from your bag"); }} className="focus-ring text-xs font-bold text-fitique-brown hover:text-fitique-plum">Remove</button></div></div></article>)}</div><Link to="/products" className="focus-ring mt-7 inline-flex items-center gap-2 text-[.68rem] font-extrabold uppercase tracking-[.1em] text-fitique-plum hover:gap-3">Continue shopping <ArrowRight size={15} /></Link><CartSupportPanel /></section><aside className="h-fit border border-fitique-line bg-fitique-paper p-5 lg:sticky lg:top-24"><p className="eyebrow text-fitique-brown">Order summary</p><dl className="mt-5 grid gap-3 text-sm"><div className="flex justify-between"><dt className="text-fitique-ink/65">Subtotal</dt><dd className="font-bold">{formatCurrency(subtotal)}</dd></div><div className="flex justify-between"><dt className="text-fitique-ink/65">Delivery</dt><dd className="font-bold">Confirmed at checkout</dd></div>{promotionCode && <div className="flex justify-between text-fitique-brown"><dt>Offer code</dt><dd className="font-bold">{promotionCode}</dd></div>}<div className="editorial-rule my-1" /><div className="flex justify-between text-base"><dt className="font-extrabold">Estimated total</dt><dd className="font-extrabold text-fitique-plum">{formatCurrency(total)}</dd></div></dl><p className="mt-4 text-xs leading-5 text-fitique-ink/60">{deliveryPreference} is saved. Your address confirms the final delivery price, timing, and eligible offers.</p><button onClick={() => navigate("/checkout")} className="plum-button focus-ring mt-6 w-full">Arrange delivery <ArrowRight size={15} /></button><div className="mt-5 flex gap-2 border-t border-fitique-line pt-4 text-xs leading-5 text-fitique-ink/60"><Heart size={15} className="mt-.5 shrink-0 text-fitique-plum" /> Your pieces stay saved in this bag on this device.</div></aside></div></div>;
}
