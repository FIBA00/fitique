/* Lavender Lookbook profile: a concise personal wardrobe record that keeps user details calm and optional. */
import { zodResolver } from "@hookform/resolvers/zod";
import { Heart, History, LogOut, MapPin, Package, Ruler, Sparkles } from "lucide-react";
import { useForm } from "react-hook-form";
import { Link, Navigate, useNavigate } from "react-router";
import { z } from "zod";
import { toast } from "sonner";
import { useSessionStore } from "../hooks/useSession";
import { usePageMeta } from "../../../hooks/usePageMeta";
import { useProfile, useUpdateProfile } from "../../user/hooks/useProfile";

const schema = z.object({ name: z.string().min(2, "Please enter your name."), email: z.string().email("Please use a valid email.") });
export default function Profile() { usePageMeta("Your profile", "Manage your Fitique profile and personal fit preferences."); const sessionUser = useSessionStore((state) => state.user); const setUser = useSessionStore((state) => state.setUser); const logout = useSessionStore((state) => state.logout); const navigate = useNavigate(); const profileQuery = useProfile(Boolean(sessionUser)); const user = profileQuery.data || sessionUser; const { register, handleSubmit, formState: { errors } } = useForm({ resolver: zodResolver(schema), values: { name: user?.name || "", email: user?.email || "" } }); const mutation = useUpdateProfile(); if (!user) return <Navigate to="/login" replace />; return <div className="content-shell py-8 lg:py-12"><div className="border-b border-fitique-line pb-7"><p className="eyebrow text-fitique-brown">Your Fitique</p><h1 className="serif mt-2 text-5xl tracking-[-.04em] text-fitique-plum">Hello, {user.firstName || user.name.split(" ")[0]}.</h1><p className="mt-3 text-sm text-fitique-ink/65">Keep the details that make your boutique experience feel more like yours.</p></div><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_.8fr]"><section className="border border-fitique-line bg-white p-5 sm:p-6"><p className="eyebrow text-fitique-brown">Personal information</p><form onSubmit={handleSubmit((values) => mutation.mutate(values, { onSuccess: (updated) => { setUser({ ...user, ...updated }); toast("Your profile has been updated"); } }))} className="mt-5 grid gap-4"><label><span className="field-label">Your name</span><input className="field-input" {...register("name")} />{errors.name && <p className="field-error">{errors.name.message}</p>}</label><label><span className="field-label">Email address</span><input className="field-input" {...register("email")} />{errors.email && <p className="field-error">{errors.email.message}</p>}</label><button disabled={mutation.isPending} className="plum-button focus-ring mt-2 w-fit disabled:opacity-60">{mutation.isPending ? "Saving…" : "Save changes"}</button></form></section><div className="grid content-start gap-3"><Link to="/orders" className="focus-ring flex items-center gap-4 border border-fitique-line bg-fitique-paper p-4 hover:border-fitique-plum"><Package size={20} className="text-fitique-plum" /><span><span className="block text-sm font-extrabold">Orders</span><span className="mt-1 block text-xs text-fitique-ink/60">Follow your current delivery.</span></span></Link><Link to="/fit-history" className="focus-ring flex items-center gap-4 border border-fitique-line bg-fitique-paper p-4 hover:border-fitique-plum"><History size={20} className="text-fitique-plum" /><span><span className="block text-sm font-extrabold">Fit Check history</span><span className="mt-1 block text-xs text-fitique-ink/60">Review and compare saved guidance.</span></span></Link><Link to="/wishlist" className="focus-ring flex items-center gap-4 border border-fitique-line bg-fitique-paper p-4 hover:border-fitique-plum"><Heart size={20} className="text-fitique-plum" /><span><span className="block text-sm font-extrabold">Saved edit</span><span className="mt-1 block text-xs text-fitique-ink/60">Your considered shortlist.</span></span></Link><div className="border border-fitique-line bg-fitique-lilac/45 p-4"><div className="flex items-center gap-3"><Ruler size={20} className="text-fitique-plum" /><p className="text-sm font-extrabold">Fit profile</p></div><div className="mt-4 grid grid-cols-2 gap-3 text-xs"><div><span className="block text-fitique-ink/55">Preferred size</span><strong className="mt-1 block text-fitique-plum">{user.fitProfile?.preferredSize || "Not set"}</strong></div><div><span className="block text-fitique-ink/55">Style</span><strong className="mt-1 block text-fitique-plum">{user.fitProfile?.style || "Not set"}</strong></div></div><Link to="/fit-check" className="focus-ring mt-4 inline-flex items-center gap-2 text-xs font-extrabold text-fitique-plum hover:underline"><Sparkles size={14} /> Refresh your recommendation</Link></div><div className="border border-fitique-line bg-fitique-paper p-4"><div className="flex items-center gap-3"><MapPin size={20} className="text-fitique-plum" /><p className="text-sm font-extrabold">{user.addresses?.[0]?.label || "Address"}</p></div><p className="mt-3 text-xs leading-5 text-fitique-ink/60">{user.addresses?.[0]?.line1}<br />{user.addresses?.[0]?.city}, {user.addresses?.[0]?.region}</p></div><button onClick={() => { logout(); toast("You have been signed out"); navigate("/"); }} className="focus-ring mt-2 flex w-fit items-center gap-2 text-xs font-extrabold text-fitique-brown hover:text-fitique-plum"><LogOut size={15} /> Sign out</button></div></div></div>; }
