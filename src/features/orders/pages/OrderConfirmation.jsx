import { Check, ChevronRight, CircleHelp, PackageCheck, Truck } from "lucide-react";
import { Link, useLocation, useParams } from "react-router";
import { useOrder } from "../../../hooks/useOrders";
import { formatCurrency } from "../../../lib/utils";
import { usePageMeta } from "../../../hooks/usePageMeta";
import PaymentHandoffCard from "../../../components/commerce/PaymentHandoffCard";

export default function OrderConfirmation() {
  const { orderId } = useParams();
  const location = useLocation();
  const { data: fetchedOrder, isLoading } = useOrder(orderId);
  const order = location.state?.order || fetchedOrder;
  usePageMeta("Order confirmed", "Review your Fitique order and delivery summary.");
  if (isLoading && !order) return <div className="content-shell grid min-h-[55vh] place-items-center"><div className="h-9 w-9 animate-spin rounded-full border-2 border-fitique-lilac border-t-fitique-plum" /></div>;
  if (!order) return <div className="content-shell py-16 text-center"><p className="eyebrow text-fitique-brown">Order unavailable</p><h1 className="serif mt-3 text-5xl text-fitique-plum">We couldn’t find that order.</h1><Link to="/orders" className="plum-button focus-ring mt-7">View your orders <ChevronRight size={15} /></Link></div>;
  const subtotal = order.items.reduce((sum, item) => sum + item.price * item.quantity, 0);
  const delivery = Math.max(0, order.total - subtotal);
  return <div className="bg-fitique-paper"><div className="content-shell py-10 lg:py-14"><section className="border-b border-fitique-line pb-9 text-center"><span className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-fitique-plum text-white"><Check size={28} /></span><p className="eyebrow mt-6 text-fitique-brown">Order confirmed · {order.id}</p><h1 className="serif mt-3 text-5xl tracking-[-.05em] text-fitique-plum sm:text-6xl">It’s on its way to you.</h1><p className="mx-auto mt-4 max-w-xl text-sm leading-7 text-fitique-ink/65">Your edit is reserved. We’ll share every delivery update as your pieces make their way to you.</p></section><div className="mt-9 grid gap-8 lg:grid-cols-[1.15fr_.85fr]"><section className="border border-fitique-line bg-fitique-ivory p-5 sm:p-7"><div className="flex items-start justify-between gap-4"><div><p className="eyebrow text-fitique-brown">Your selected pieces</p><h2 className="serif mt-2 text-3xl text-fitique-plum">The order summary</h2></div><PackageCheck className="text-fitique-plum" size={24} /></div><div className="mt-6 divide-y divide-fitique-line border-y border-fitique-line">{order.items.map((item) => <article key={`${item.productId}-${item.size}-${item.color}`} className="flex gap-4 py-4"><img src={item.image} alt={item.name} className="h-24 w-20 object-cover brightness-[.97] saturate-[.65] sepia-[.08]" /><div className="min-w-0 flex-1"><p className="text-sm font-extrabold">{item.name}</p><p className="mt-1 text-xs text-fitique-ink/55">{item.color} · {item.size} · Quantity {item.quantity}</p><p className="mt-3 text-sm font-bold text-fitique-plum">{formatCurrency(item.price * item.quantity)}</p></div></article>)}</div><dl className="mt-6 grid gap-3 text-sm"><div className="flex justify-between"><dt>Subtotal</dt><dd>{formatCurrency(subtotal)}</dd></div><div className="flex justify-between"><dt>Delivery</dt><dd>{formatCurrency(delivery)}</dd></div><div className="flex justify-between border-t border-fitique-line pt-3 font-extrabold text-fitique-plum"><dt>Total due</dt><dd>{formatCurrency(order.total)}</dd></div></dl></section><aside className="grid h-fit gap-5"><section className="border border-fitique-line bg-fitique-lilac/45 p-5"><Truck size={24} className="text-fitique-plum" /><p className="eyebrow mt-5 text-fitique-brown">Delivery details</p><h2 className="serif mt-2 text-3xl text-fitique-plum">{order.delivery.method}</h2><p className="mt-3 text-sm font-bold text-fitique-ink">{order.delivery.window}</p><p className="mt-2 text-sm leading-6 text-fitique-ink/65">{order.delivery.address}</p><Link to={`/orders/${order.id}`} className="focus-ring mt-5 inline-flex items-center gap-1 text-xs font-extrabold uppercase tracking-[.08em] text-fitique-plum hover:underline">Track delivery <ChevronRight size={14} /></Link></section><PaymentHandoffCard order={order} /><section className="border border-fitique-line bg-fitique-ivory p-5"><CircleHelp size={22} className="text-fitique-brown" /><p className="serif mt-4 text-2xl text-fitique-plum">Need to adjust something?</p><p className="mt-2 text-sm leading-6 text-fitique-ink/65">Visit order tracking to see the latest status, delivery timing, and return options.</p><Link to="/orders" className="ghost-button focus-ring mt-5">Your orders <ChevronRight size={14} /></Link></section></aside></div><div className="mt-10 text-center"><Link to="/products" className="plum-button focus-ring">Continue the edit <ChevronRight size={15} /></Link></div></div></div>;
}
